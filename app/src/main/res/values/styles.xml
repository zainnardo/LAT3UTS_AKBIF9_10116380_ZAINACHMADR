<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/oldgold</item>
        <item name="colorPrimaryDark">@color/orangeold</item>
        <item name="colorAccent">@color/younggold</item>
    </style>

</resources>
